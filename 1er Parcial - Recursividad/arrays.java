import java.util.ArrayList;
/**
 * Write a description of class arrays here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class arrays
{
    public ArrayList<Integer> generarArray(int a, int b, int c){
        ArrayList<Integer>res = new ArrayList();
        res.add(a);
        res.add(b);
        res.add(c);
        return res;
    }
}
